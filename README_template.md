# 🛍️ E-Commerce Demo – Vue 3 + Supabase + Vercel

## 1. Mục tiêu dự án

Website demo **bán hàng quần áo/phụ kiện** gồm:

- Site cho khách hàng (client).
- Dashboard đơn giản cho admin (mock logic trên frontend).
- Lưu thông tin **người dùng & đơn hàng** trên **Supabase (Postgres)**.
- Dữ liệu sản phẩm có thể hard-code trong code (mock) ở giai đoạn demo.

Triển khai frontend bằng **Vue 3 + Vite**, deploy trên **Vercel**.  
Supabase cung cấp **DB + Auth + REST API** cho user & order (free tier đủ cho demo).[web:13][web:16][web:19]

---

## 2. Công nghệ chính

### Frontend

- **Vue 3** (Composition API) + **Vite**.[web:1][web:7]
- **Vue Router** cho routing.
- **Pinia** hoặc store tự viết để quản lý cart, user, settings.

### Backend (BaaS)

- **Supabase**:
  - Database Postgres: bảng `users`, `orders`, có thể thêm `profiles` nếu cần.[web:13][web:16]
  - Auth: email/password cho client.
  - REST / JS client SDK để gọi từ Vue.[web:13]

### Deploy

- **Vercel**:
  - Dùng preset **Vite** cho Vue 3.[web:5][web:8]
  - Build command: `npm run build`
  - Output directory: `dist`

---

## 3. Supabase – Cấu hình & Bảng dữ liệu

### 3.1. Thiết lập Supabase

1. Tạo project trên Supabase.
2. Lấy các env:

   - `NEXT_PUBLIC_SUPABASE_URL`
   - `NEXT_PUBLIC_SUPABASE_ANON_KEY`

3. Thêm vào `.env` (local) và Vercel Project Environment Variables.

### 3.2. Bảng & Auth

#### Auth (built-in Supabase)

- Sử dụng Supabase Auth cho **client login** bằng email/password.[web:13]
- Admin demo vẫn **mock** (hard-code trên frontend).

#### Bảng đề xuất

1. `profiles` (user info mở rộng)

- Liên kết với Supabase auth user:

```sql
create table if not exists public.profiles (
  id uuid primary key references auth.users(id) on delete cascade,
  full_name text,
  phone text,
  address text,
  created_at timestamp with time zone default timezone('utc'::text, now())
);
orders (đơn hàng)

sql
create table if not exists public.orders (
  id bigint generated by default as identity primary key,
  user_id uuid references auth.users(id),
  email text not null,
  total_amount numeric(10, 2) not null,
  status text default 'pending', -- pending, cancelled, paid
  raw_cart jsonb not null,       -- lưu chi tiết cart (id sản phẩm, size, màu...)
  created_at timestamp with time zone default timezone('utc'::text, now())
);
Giai đoạn demo: sản phẩm có thể không lưu vào DB, chỉ cần raw_cart chứa data đầy đủ.

4. Ảnh & Data sản phẩm
4.1. Ảnh sản phẩm
Lưu trực tiếp trong repo (public), ví dụ:

text
/public/assets/upload/12/main-image.png
/public/assets/upload/12/gallery-1.png
/public/assets/upload/12/gallery-2.png
Trường mainImage, gallery[] trong code trỏ đến các path này.

4.2. Data sản phẩm (mock)
Không dùng file JSON rời; dùng module TS/JS trong source, ví dụ:

ts
// src/mock/products.ts
export const products = [
  {
    id: 12,
    slug: 'ao-thun-basic-trang',
    name: 'Áo thun basic trắng',
    category: 'ao',
    price: 199000,
    status: 'in_stock',
    quantity: 10,
    variants: [
      { size: 'S', color: 'white' },
      { size: 'M', color: 'white' },
    ],
    mainImage: '/assets/upload/12/main-image.png',
    gallery: [
      '/assets/upload/12/gallery-1.png',
      '/assets/upload/12/gallery-2.png',
    ],
    descriptionHtml: '<h2>Mô tả</h2><p>Áo thun form rộng...</p>',
    externalLinks: {
      youtube: 'https://youtube.com/...',
      tiktok: 'https://www.tiktok.com/...'
    }
  }
];
5. Tính năng – Admin (Dashboard demo)
Dashboard chỉ chạy trên frontend: không phân quyền thật; login admin mock.

5.1. Đăng nhập Admin
Route: /admin/login

Tài khoản mặc định hard-code:

user: admin

password: Quanlyad

Nếu đúng → set flag (ví dụ: localStorage.setItem('isAdmin', 'true')) → redirect /admin/dashboard.

5.2. Module trong Dashboard
Quản lý danh mục (demo)

Data danh mục có thể để chung trong module src/mock/categories.ts.

CRUD chỉ lưu trên state (không cần sync Supabase trong bản demo).

Quản lý sản phẩm (demo)

Form thêm / chỉnh sửa sản phẩm (dựa trên cấu trúc products).

Cập nhật state runtime (không cần lưu vào Supabase trong v1).

Ảnh: chọn từ list path sẵn /assets/upload/... (không cần upload thật).

Quản lý khách hàng (demo)

Đọc danh sách user từ Supabase auth.users hoặc profiles.

Chỉ cần view list cơ bản.

Quản lý đơn hàng (thật)

Đọc từ bảng orders trên Supabase.

Admin có thể cập nhật status (pending → cancelled / paid).

UI Settings (demo)

Lưu trong LocalStorage hoặc Pinia (không cần Supabase):

Tên website, logo path.

Danh sách main visuals.

Bật/tắt section trên trang Home.

6. Tính năng – Client Site
6.1. Layout
Header

Bên trái: logo (click về /).

Nếu không có logo → hiển thị tên website.

Bên phải:

Icon giỏ hàng (số lượng items).

Icon user: nếu chưa login → link /auth/login, nếu login → /account.

Footer

Hiển thị thông tin liên hệ (IG, TikTok, email, v.v.).

6.2. Các page chính
Home – /

Main Visual (1–6 ảnh; nếu không có → ẩn section).

Section danh mục.

Section sản phẩm nổi bật (dùng subset từ products).

Product List – /products

Hiển thị tất cả products.

Bộ lọc theo category.

Product Detail – /products/:slug

Tìm sản phẩm theo slug.

Hiển thị:

Tên, giá, trạng thái.

Ảnh chính + gallery.

Biến thể size/màu.

Mô tả HTML.

Nút Add to Cart → thêm vào cart (Pinia + LocalStorage).

Cart – /cart

Danh sách sản phẩm trong cart.

Tổng tiền, update quantity, remove item.

Nút Thanh toán → /checkout.

Checkout – /checkout

Form:

Họ tên, SĐT, địa chỉ, email, ghi chú.

Nếu client đã login (Supabase Auth):

Prefill từ profiles.

Nếu chưa login:

Email + password: tạo account Supabase (signup).

Password mặc định có thể đề xuất trong UI là xincamon123 hoặc để user tự nhập (nhưng spec dự án ban đầu: tạo với password mặc định xincamon123).

Sau khi submit:

Tạo bản ghi orders trong Supabase với raw_cart từ cart.

Clear cart.

Redirect /account.

Account – /account

Gọi Supabase để lấy:

profiles (thông tin KH).

Danh sách orders của user hiện tại.

Hiển thị thông báo:

“Vui lòng liên hệ về A qua IG hoặc TIKTOK để Thanh toán trong 12h, nếu không đơn hàng sẽ tự động huỷ và sản phẩm sẽ được bán cho người khác.”

7. Cấu trúc thư mục gợi ý
text
src/
  main.ts
  App.vue
  router/
    index.ts
  lib/
    supabaseClient.ts      # khởi tạo Supabase client
  mock/
    products.ts
    categories.ts
  store/ (Pinia)
    cartStore.ts
    userStore.ts
    settingsStore.ts
  components/
    layout/
      AppHeader.vue
      AppFooter.vue
    products/
      ProductCard.vue
      ProductGrid.vue
      ProductFilter.vue
    admin/
      AdminLoginForm.vue
      AdminDashboardLayout.vue
  pages/
    HomePage.vue
    ProductsListPage.vue
    ProductDetailPage.vue
    CartPage.vue
    CheckoutPage.vue
    AccountPage.vue
    auth/
      LoginPage.vue
      RegisterPage.vue (nếu cần)
    admin/
      AdminLoginPage.vue
      AdminDashboardPage.vue

public/
  assets/
    upload/
      12/
        main-image.png
        gallery-1.png
        gallery-2.png
